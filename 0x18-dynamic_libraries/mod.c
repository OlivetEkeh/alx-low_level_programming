#include <stdio.h>
#include <stdlib.h>

/**
 * mod - modulus of two numbers  
 * @a: the first int
 * @b: the second int
 * Return: the modulus of the integers
 */
int mod(int a, int b)
{
	return (a % b);
}
