#include <stdio.h>
#include <stdlib.h>

/**
 * sub - This would sub two integers 
 * @a: the first int
 * @b: the second int
 * Return: the subtraction of the integers
 */
int sub(int a, int b)
{
	return (a - b);
}
