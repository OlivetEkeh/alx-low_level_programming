#include <stdio.h>
#include <stdlib.h>

/**
 * mul - This would multiply two integers 
 * @a: the first int
 * @b: the second int
 * Return: the multiplication of the integers
 */
int mul(int a, int b)
{
	return (a * b);
}
